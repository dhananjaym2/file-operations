#!bin/bash
# Program to search-specified-pattern-and-copy-matching-line-to-new-file

# read input user specified file name, check if it exists

# read output file name, check if it exists, then prompt user to replace it.

# read a line into a buffer and search for that specifed pattern in that line

# if matched pattern is found then append that to the copy file

# Prompt user that operation is complete.
echo "Operation complete."
